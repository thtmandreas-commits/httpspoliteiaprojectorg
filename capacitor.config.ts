import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.politeia.doomloop',
  appName: 'The Great Doom Loop',
  webDir: 'dist',
};

export default config;
